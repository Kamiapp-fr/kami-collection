import { resolve } from 'path';
import { defineConfig } from 'vite';
import dts from 'vite-plugin-dts';

module.exports = defineConfig({
  build: {
    lib: {
      entry: resolve(__dirname, 'src/kami-{{lowerCase name}}.ts'),
      name: 'Kami{{pascalCase name}}',
      fileName: (format) => `kami-{{lowerCase name}}.${format}.js`
    },
  },
  plugins: [dts()]
})