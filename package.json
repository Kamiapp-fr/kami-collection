{
  "name": "@kamiapp/collection",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite dev",
    "build:demos": "tsc && vite build",
    "build:packages": "npm run build --workspaces",
    "publish:packages": "npm publish --workspaces",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "workspaces": [
    "./packages/component",
    "./packages/flash",
    "./packages/infinite-list"
  ]
}
