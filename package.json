{
  "name": "@kamiapp/collection",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite dev",
    "build": "tsc && vite build",
    "test": "echo '@TODO add tests'",
    "lint": "echo '@TODO add lint'",
    "preview": "vite preview",
    "version:patch": "npm version patch --workspaces && git add . && npm version patch --force",
    "version:minor": "npm version minor --workspaces && git add . && npm version minor --force",
    "version:major": "npm version major --workspaces && git add . && npm version major --force"
  },
  "devDependencies": {
    "@geut/chan": "^3.2.6",
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "workspaces": [
    "./packages/*"
  ]
}
